import React, { Component } from 'react';
import CanvasView from './CanvasView';
import './App.css';

/**
 *	Right now, just renders the CanvasView.
 */
class App extends Component {
  render() {
    return (
      <CanvasView />
    );
  }
}

export default App;
